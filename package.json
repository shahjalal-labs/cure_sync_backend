{
  "name": "cure_sync_backend",
  "module": "index.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only index.ts"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1"
  }
}
